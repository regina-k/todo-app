# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Repository status

This repository is currently empty — no source files, no build configuration, no version control history. Future Claude Code sessions should expect to scaffold the project from scratch based on the user's instructions.

## Notes for future sessions

- Re-run `/init` once the project has actual code so this file can be filled in with real build/test/lint commands and an architecture overview.
- The working directory is `C:\Users\campus1N005\Desktop\Project1` on Windows; default shell is PowerShell.

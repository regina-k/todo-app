# 📋 나의 할 일

순수 HTML / CSS / JavaScript 로 만든 개인용 할 일 관리 웹앱입니다.
빌드 도구나 별도 서버 없이 브라우저에서 바로 실행되며, 데이터는 로컬 브라우저에만 저장됩니다.

## 주요 기능

- 할 일 추가 / 수정(인라인 편집) / 삭제
- 완료 토글 (체크박스 + 취소선)
- 카테고리 분류: 업무 / 개인 / 공부 (색상 구분)
- 카테고리별 필터링 + 활성 필터 시각 표시
- 진행률 표시: 프로그레스 바 + `X / Y 완료 (Z%)` 텍스트 (필터와 무관하게 전체 기준)
- 빈 상태 안내 메시지
- 새로고침 후에도 데이터 유지 (localStorage)
- 모바일(≤ 480px) 반응형 레이아웃

## 실행 방법

별도의 빌드나 패키지 설치가 필요 없습니다.

**방법 1 — 파일 직접 열기**

`index.html`을 파일 탐색기에서 더블클릭하거나, 브라우저 주소창에 파일 경로를 입력합니다.

**방법 2 — 로컬 서버 (권장)**

`file://` 프로토콜에서는 일부 브라우저가 storage / 보안 정책을 더 엄격히 적용할 수 있어, 가능하면 로컬 서버로 띄우는 것을 권장합니다.

```bash
# Python 3
python3 -m http.server 8000

# Node.js
npx serve
```

이후 브라우저에서 `http://localhost:8000` 으로 접속.

## 데이터 저장

모든 할 일은 브라우저의 **localStorage** 에 `todos` 키로 JSON 저장됩니다.

- 같은 브라우저·같은 도메인(또는 같은 파일 경로)에서만 데이터가 유지됩니다.
- 브라우저의 사이트 데이터를 삭제하거나 시크릿 모드로 열면 데이터는 보존되지 않습니다.
- 다른 기기·브라우저 간 동기화는 지원하지 않습니다.

콘솔에서 직접 확인:

```js
JSON.parse(localStorage.getItem("todos"));
```

## 파일 구조

```
Project1/
├── index.html   # DOM 골격 (헤더 / 진행률 / 입력 / 필터 / 목록)
├── style.css    # 스타일 + 카테고리 색상 + 반응형
├── app.js       # 데이터 / 렌더링 / 이벤트 / 초기화
└── README.md    # 본 문서
```

## 향후 추가할 만한 기능 아이디어

1. **마감일 / 알림** — 각 할 일에 due date 필드를 추가하고, 임박한 항목을 강조하거나 브라우저 알림으로 띄우기
2. **드래그 앤 드롭 정렬** — 항목 순서를 사용자가 직접 바꾸고 그 순서까지 localStorage에 보존
3. **데이터 내보내기 / 가져오기** — JSON 파일로 백업·복원하거나 다른 기기로 옮길 수 있는 기능 (현재는 브라우저 종속이라 유실 위험)
